# Local MySQL container only. Copy to .env and change values if needed.
META_AGENT_DB_NAME=meta_agent
META_AGENT_DB_USER=meta_agent
META_AGENT_DB_PASSWORD=change-me-local
META_AGENT_DB_ROOT_PASSWORD=change-root-local

# Do not commit real model keys.
# DEEP_SEEK_API_KEY=
# META_AGENT_MASTER_KEY=

